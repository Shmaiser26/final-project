/* import {pushed} from "./setActiveKeys";

const resultString = document.querySelector(".result-string");
const startTypingBtn = document.querySelector(".start-btn");

const buffer = {
  key: []
};

 const typing = () => {
   let symbol = pushed.key.textContent;
    buffer.key.push(symbol);
   resultString.textContent = buffer.key;
}


window.addEventListener("keydown", typing);



export {startTypingBtn}; */